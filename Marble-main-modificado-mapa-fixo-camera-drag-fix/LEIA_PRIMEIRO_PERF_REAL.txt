Versão mapa fixo completo + correção real de peso

O que eu encontrei no arquivo anterior:
1. Mesmo com 1 jogador, o main.gd ainda fazia:
   ensure_marble_pool(MAX_MARBLES)
   e MAX_MARBLES era 300.
   Ou seja: criava 300 bolinhas no início.

2. A tela de espera ainda gerava a pista 3D antes da corrida:
   open_site_waiting() chamava _race.generate_race(true).

3. O botão Sair do menu ainda tinha get_tree().quit().

O que foi corrigido:
1. MAX_MARBLES agora é 180.
2. No _ready(), o jogo cria só:
   max(Bridge.jogadores.size(), 1)
3. ensure_marble_pool agora é dinâmica:
   - remove bolinhas extras
   - cria só a quantidade real necessária
   - pausa todas até iniciar
4. open_site_waiting não gera mais pista pesada.
   A pista só aparece ao iniciar a corrida.
5. load_site_players ajusta o pool para a quantidade real.
6. start_site_race garante pool correto antes de iniciar.
7. get_tree().quit() foi neutralizado no menu.
8. Mantido:
   - mapa fixo completo
   - colisão entre bolinhas
   - velocidade 3x
   - ranking com scroll
   - clique no ranking para seguir bolinha
   - render scale 0.7
   - anti-aliasing desligado

Teste:
1. Abra no Godot.
2. Aperte F5.
3. Exporte Web como index.html.
4. Thread Support desativado.
5. Teste com 1 jogador.
6. No Chrome Task Manager, confira se não fica criando 1GB/CPU 100%.
